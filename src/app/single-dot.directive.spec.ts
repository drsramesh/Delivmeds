import { SingleDotDirective } from './single-dot.directive';

describe('SingleDotDirective', () => {
  it('should create an instance', () => {
    // const directive = new SingleDotDirective();
    // expect(directive).toBeTruthy();
  });
});
